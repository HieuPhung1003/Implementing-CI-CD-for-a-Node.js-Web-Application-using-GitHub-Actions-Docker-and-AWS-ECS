{{> header}}

<div class="auth-container">
    <div class="auth-card glass-pane">
        <div class="auth-header">
            <h1 class="auth-title">Đăng Ký</h1>
            <p class="auth-subtitle">Tạo tài khoản mới để bắt đầu quản lý kho</p>
        </div>

        {{#if error}}
       
        {{/if}}

        <form action="/register" method="POST" class="auth-form">
            <div class="form-group">
                <label class="form-label">Tên đăng nhập</label>
                <input type="text" name="username" class="form-control" placeholder="Chọn tên đăng nhập duy nhất" required autofocus>
                <small class="form-helper">Tên đăng nhập phải duy nhất và có ít nhất 3 ký tự</small>
            </div>

            <div class="form-group">
                <label class="form-label">Mật khẩu</label>
                <input type="password" name="password" class="form-control" placeholder="Nhập mật khẩu mạnh" required>
                <small class="form-helper">Mật khẩu sẽ được mã hóa an toàn</small>
            </div>

            <button type="submit" class="btn btn-primary btn-block">Tạo Tài Khoản</button>
        </form>

        <div class="auth-divider">hoặc</div>

        <p class="auth-footer">
            Đã có tài khoản? <a href="/login" class="auth-link">Đăng nhập</a>
        </p>
    </div>

    <div class="auth-info">
        <h2>Bắt đầu miễn phí</h2>
        <ul class="info-list">
            <li>Tài khoản an toàn với mật khẩu mã hóa</li>
            <li>Quản lý kho hàng riêng cho từng người dùng</li>
            <li>Không cần phí đăng ký</li>
            <li>Lưu trữ dữ liệu vô hạn</li>
        </ul>
    </div>
</div>

{{> footer}}